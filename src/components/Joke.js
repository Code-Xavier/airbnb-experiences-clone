function Joke(props) {
  const colors = ["red", "green", "yellow", "blue", "orange"];
  return (
    <div>
      {/* <h2>{props.setup}</h2>
        <p>{props.punchline}</p> */}
    </div>
  );
}

export default Joke;
